# =============================================================================
#  Sistema de Restaurante - Docker Compose (desarrollo)
#
#  El stack está en docker-compose.restaurante.yml (puertos 8200, 13311, 5175).
#  Este archivo solo lo incluye, para que un `docker compose up -d` a secas
#  levante el restaurante y NUNCA el proyecto `ventas-dev`.
#
#  Antes este archivo era el del sistema de ventas copiado tal cual: mismo nombre
#  de proyecto (`ventas-dev`), mismos contenedores y el MISMO volumen de datos.
#  Levantarlo desde aquí era trabajar sobre la base de ventas (19/09/2026).
# =============================================================================

name: restaurante-dev

include:
  - docker-compose.restaurante.yml
